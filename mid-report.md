# 중간 보고서 (Mid Report)

## 프로젝트명
카테고리별 일별 수요 예측 기반 발주량 최적화 시스템

---

# 1. 프로젝트 개요

본 프로젝트는 브라질 이커머스(Olist) 데이터를 활용하여 카테고리별 일별 수요를 예측하고, 예측 결과를 기반으로 발주량을 결정하여 운영 비용을 최소화하는 것을 목표로 한다.

일반적인 수요 예측 프로젝트는 예측 정확도 자체에 집중하는 경우가 많다. 그러나 실제 기업 운영에서는 예측 결과가 재고 관리 및 발주 전략에 어떻게 활용되는지가 더욱 중요하다.

따라서 본 프로젝트는 단순히 “어떤 모델의 예측 정확도가 높은가”를 비교하는 것이 아니라, 예측 결과가 실제 운영 비용 절감에 얼마나 기여할 수 있는지를 분석하는 데 초점을 둔다.

즉, 본 프로젝트는 다음과 같은 흐름을 가진다.

```text
수요 예측
→ 발주량 결정
→ 재고 비용 및 품절 비용 계산
→ 모델별 운영 비용 비교
```

---

# 2. 프로젝트 핵심 질문

본 프로젝트는 다음 질문을 중심으로 진행된다.

> “수요 예측 모델이 실제 발주 전략과 운영 비용 절감에 얼마나 기여할 수 있는가?”

이를 위해 다음과 같은 세부 질문을 함께 분석한다.

- 카테고리별 수요 패턴은 서로 차이가 존재하는가?
- 하나의 모델이 모든 카테고리에 항상 최적이라고 볼 수 있는가?
- 예측 정확도가 높을수록 실제 운영 비용도 반드시 감소하는가?
- 어떤 모델이 실제 운영 관점에서 가장 효율적인가?
- 비용 관점에서 카테고리별 최적 전략이 달라질 수 있는가?

---

# 3. 데이터셋 및 데이터 구조

본 프로젝트는 Kaggle의 Brazilian E-Commerce Public Dataset by Olist 데이터를 활용한다.

분석 과정에서는 Python(Pandas)을 활용하여 관계형 CSV 데이터를 통합하였다.

사용한 주요 테이블은 다음과 같다.

| 테이블 | 역할 |
|---|---|
| orders | 주문 날짜 정보 |
| order_items | 주문 상품 및 배송비 정보 |
| products | 상품 카테고리 정보 |

각 테이블을 `pd.merge()`를 활용하여 결합하고, 주문 데이터를 일별(Daily) 기준으로 집계하여 다음 형태의 시계열 데이터를 생성하였다.

| date | category | demand |
|---|---|---|
| 2018-01-01 | electronics | 53 |
| 2018-01-01 | toys | 17 |

즉, “카테고리별 일별 주문량”을 최종 예측 대상으로 설정하였다.

---

# 4. 현재까지 진행 내용

## 4-1. 데이터 전처리 및 EDA 수행

현재까지 데이터 로드, 테이블 병합, 결측치 처리 및 기본적인 탐색적 데이터 분석(EDA)을 완료하였다.

진행한 주요 작업은 다음과 같다.

- 주문 데이터 datetime 변환
- 카테고리별 일별 주문량 집계
- 결측 카테고리 처리
- 주문이 없는 날짜에 대한 0 보간
- 카테고리별 수요 패턴 분석
- 배송비(freight_value) 분포 분석

---

## 4-2. EDA 결과 및 주요 인사이트

### (1) 카테고리별 수요 패턴 차이 존재

카테고리별로 수요 특성이 크게 다르게 나타났다.

특히 다음 요소에서 차이가 확인되었다.

- 수요 변동성(CV)
- 주문이 발생하지 않는 날짜 비율(Zero Rate)
- 계절성 및 특정 시기 집중 현상

일부 상위 카테고리는 비교적 안정적인 수요 흐름을 보였으나, 일부 카테고리는 주문량 자체가 매우 적고 Sparse한 형태를 보였다.

이는 일반적인 시계열 모델 적용 시 예측 안정성이 낮아질 가능성이 있음을 의미한다.

이를 통해 하나의 모델을 모든 카테고리에 동일하게 적용하는 방식에는 한계가 있을 수 있음을 확인하였다.

즉:

- 안정적인 수요 패턴에는 ARIMA와 같은 전통 시계열 모델이 적합할 가능성이 있으며,
- 변동성이 크고 비선형 패턴이 존재하는 카테고리에는 XGBoost 또는 LSTM 기반 접근이 더 적합할 가능성이 존재한다.

---

### (2) 시계열 패턴 존재 확인

상위 카테고리에서는 다음과 같은 패턴이 관찰되었다.

- 특정 요일 주문량 증가
- 월별 수요 차이
- 기간별 수요 변동성

이를 통해 시계열 기반 수요 예측이 충분히 가능함을 확인하였다.

또한 향후 Feature Engineering 단계에서 다음 변수를 활용할 수 있음을 확인하였다.

- Lag Feature
- Rolling Mean
- Weekday
- Month

---

### (3) 비용 구조 차이 가능성 확인

배송비(freight_value) 분포를 확인한 결과 카테고리별 차이가 존재하였다.

이를 통해 모든 카테고리에 동일한 비용 파라미터를 적용할 경우 실제 운영 특성을 충분히 반영하지 못할 가능성이 있음을 확인하였다.

예를 들어:

- 배송비가 높은 카테고리는 재고 유지 비용(Holding Cost)의 영향이 더 클 수 있으며,
- 주문 빈도가 낮고 Zero Rate가 높은 카테고리는 품절 비용(Stockout Cost)에 더 민감할 수 있다.

따라서 향후 비용 시뮬레이션 단계에서는 카테고리별 특성을 고려한 비용 구조 적용 가능성을 함께 검토할 예정이다.

---

# 5. 목표 수정 및 프로젝트 방향 재정립

초기 프로젝트 계획에서는 다수의 예측 모델과 복잡한 시스템 구조를 모두 포함하려 하였다.

그러나 실제 프로젝트 기간과 구현 난이도를 고려했을 때 범위가 과도하게 넓다는 문제가 존재하였다.

또한 “실제로 무엇을 분석하려는 프로젝트인가?”라는 핵심 질문이 다소 모호하다는 피드백을 받았다.

이에 따라 프로젝트 목표를 다음과 같이 재정립하였다.

> > “누적 주문량 기준 상위 카테고리를 대상으로 일별 수요를 예측하고, 예측 결과를 기반으로 실제 운영 비용 절감 효과를 비교 분석한다.”

즉, 단순히 예측 정확도를 비교하는 것이 아니라:

- 예측 결과가 발주 전략에 어떤 영향을 미치는지
- 과다 예측과 과소 예측이 어떤 비대칭적 비용 차이를 발생시키는지
- 어떤 모델이 카테고리 특성에 따라 실제 운영 측면에서 더 효율적인지

를 분석하는 방향으로 프로젝트를 구체화하였다.

---

# 6. 예측 모델 구성

현재 프로젝트에서는 다음 모델들을 중심으로 구현을 진행한다.

| 모델 | 역할 |
|---|---|
| ARIMA | 전통적 시계열 Baseline 모델 |
| XGBoost | Feature 기반 머신러닝 모델 |
| LSTM | 시계열 딥러닝 모델 |

각 모델은 다음과 같은 특징을 가진다.

### ARIMA
시계열의 추세와 자기상관성을 기반으로 수요를 예측한다.

### XGBoost
Lag 및 Rolling 기반 Feature를 활용하여 비선형 패턴을 학습한다.

### LSTM
장기 의존성과 복잡한 시계열 패턴을 학습하기 위한 딥러닝 모델이다.

단, 프로젝트 일정에 따라 ARIMA와 XGBoost를 우선적으로 안정화한 뒤 후속 적용한다.

---

# 7. 비용 함수 및 평가 방식

본 프로젝트는 단순 예측 정확도뿐 아니라 실제 운영 비용 관점에서도 모델을 평가한다.

---

## 7-1. 비용 함수

총 운영 비용(TotalCost)은 다음과 같이 정의한다.

```text
TotalCost = HoldingCost + StockoutCost
```

### 재고 유지 비용 (Holding Cost)

```text
HoldingCost = max(Order - Demand, 0) × h
```

- 초과 발주된 재고에 대한 보관 비용
- `h` : 단위당 재고 유지 비용 파라미터

### 품절 비용 (Stockout Cost)

```text
StockoutCost = max(Demand - Order, 0) × s
```

- 수요보다 적게 발주하여 발생한 손실 비용
- `s` : 단위당 품절 손실 비용 파라미터

실무적 특성을 반영하여 일반적으로 다음과 같이 가정한다.

```text
s > h
```

이는 품절로 인한 판매 기회 손실이 일반적인 재고 보관 비용보다 더 크게 작용할 수 있기 때문이다.

---

## 7-2. 평가 지표

### 예측 성능 평가

| 지표 | 의미 |
|---|---|
| SMAPE | 예측 오차 비율 측정 |
| RMSE | 큰 오차에 대한 민감도 평가 |

### 운영 성능 평가

| 지표 | 의미 |
|---|---|
| Holding Cost | 과잉 재고 비용 |
| Stockout Cost | 품절 손실 비용 |
| Total Cost | 총 운영 비용 |

---

# 8. 향후 일정

| 기간 | 진행 내용 |
|---|---|
| 1주 차 | Lag/Rolling Feature 생성 및 시계열 데이터 정제 |
| 2주 차 | ARIMA 및 XGBoost 구현 및 성능 비교 |
| 3주 차 | LSTM 적용 및 비용 시뮬레이션 구축 |
| 최종 단계 | 결과 시각화 및 최종 보고서 작성 |

---

# 9. 기대 결과

본 프로젝트를 통해 다음 내용을 확인하고자 한다.

- 카테고리별 수요 특성 차이 분석
- 예측 정확도와 실제 운영 비용 간 관계 분석
- 데이터 기반 발주 전략 가능성 검증
- 시계열 예측과 비용 최적화 의사결정의 결합 가능성 확인

또한 단순 모델 비교를 넘어, 실제 운영 의사결정과 연결 가능한 데이터 분석 프로젝트 구현을 목표로 한다.

---
