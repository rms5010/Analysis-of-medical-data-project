# 프로젝트 중간 방향 정리

# 프로젝트명
카테고리별 일별 수요 예측 기반 발주량 최적화 시스템

---

# 1. 프로젝트 목적

본 프로젝트의 목적은 브라질 이커머스(Olist) 주문 데이터를 활용하여 카테고리별 일별 수요를 예측하고, 예측 결과를 기반으로 최적 발주량을 결정하여 재고 비용과 품절 비용을 최소화하는 것이다.

기존의 수요 예측 프로젝트는 예측 정확도 자체에만 집중하는 경우가 많았으나, 실제 기업 운영에서는 예측 결과가 재고 관리 및 발주 의사결정에 어떻게 활용되는지가 더욱 중요하다.

따라서 본 프로젝트는 단순 예측 모델 비교를 넘어, 예측 결과가 실제 운영 비용 절감에 얼마나 기여할 수 있는지를 분석하는 것을 목표로 한다.

---

# 2. 프로젝트 핵심 질문

본 프로젝트는 다음 질문을 중심으로 진행된다.

> “수요 예측 모델이 실제 발주 전략과 운영 비용 절감에 얼마나 기여할 수 있는가?”

또한 다음과 같은 세부 질문을 함께 분석한다.

* 카테고리별 수요 패턴은 서로 차이가 존재하는가?
  * 예: 일정한 수요 패턴 vs Sparse한 수요 패턴

* 예측 정확도가 높을수록 실제 운영 비용도 반드시 감소하는가?
  * 예: 동일한 예측 오차라도 과소 예측과 과대 예측의 비용 영향 차이 분석

* 어떤 예측 모델이 실제 발주 전략 측면에서 가장 효율적인가?

---

# 3. 분석 대상 및 데이터 구조

본 프로젝트는 Kaggle의 Brazilian E-Commerce Public Dataset by Olist 데이터를 활용한다.

프로젝트의 일관성과 재현 가능성을 확보하기 위해 Python(Pandas) 기반 데이터 통합 파이프라인을 구축한다.

## 3-1. 핵심 테이블 결합 구조

여러 관계형 테이블 중 다음 3개의 핵심 테이블을 중심으로 분석을 진행한다.

| 테이블 | 주요 컬럼 | 역할 |
|---|---|---|
| orders | order_id, order_purchase_timestamp | 주문 날짜 정보 |
| order_items | order_id, product_id, freight_value | 주문 상품 및 배송비 정보 |
| products | product_id, product_category_name | 상품 카테고리 정보 |

위 데이터를 `pd.merge()`를 활용하여 결합하고, 일별(Daily) 단위로 Grouping하여 다음 형태의 시계열 데이터를 생성한다.

| date | category | demand |
|---|---|---|
| 2018-01-01 | electronics | 53 |

즉, “카테고리별 일별 주문량”을 최종 예측 대상으로 설정한다.

---

# 4. 진행 내용 (현재까지)

## 4-1. 데이터 구조 분석 (EDA) 및 전처리 수행

현재 Python 환경에서 기본적인 데이터 구조 분석(EDA)을 완료하였다.

### 수행 내용

* 주요 테이블 구조 및 결측치 확인
  * `product_category_name`의 일부 결측치는 비즈니스 도메인을 고려하여 `others` 카테고리로 통합 처리

* 시계열 데이터셋 생성
  * 2016~2018년 주문 데이터를 일별 타임스탬프로 정렬
  * 주문이 없는 날짜는 0으로 보간(Imputation)하여 시계열 연속성 확보

* 카테고리별 수요 패턴 분석
  * 상위 카테고리는 비교적 명확한 요일별/월별 패턴 존재
  * 하위 카테고리는 Sparse한 형태를 보여 모델별 대응 전략 필요성 확인

### 현재까지 확인된 주요 특징

* 카테고리별 수요 편차가 크게 존재함
* 일부 카테고리는 시계열 예측이 어려울 정도로 Sparse함
* 주문량 변동성이 존재하여 수요 예측의 필요성이 확인됨
* 배송비 분포 편차가 커 비용 함수에 활용 가능함

---

# 5. 예측 모델 구성

프로젝트 범위와 구현 가능성을 고려하여 다음 모델들을 단계적으로 비교 분석한다.

| 모델 | 역할 및 특징 |
|---|---|
| ARIMA | 자기상관성과 추세를 반영하는 전통적 시계열 Baseline 모델 |
| XGBoost | Lag Feature 및 Rolling Window 기반 비선형 패턴 학습 모델 |
| LSTM | 장기 의존성과 복잡한 시계열 패턴 학습을 위한 딥러닝 모델 |

---

## 5-1. 모델 적용 전략

### ARIMA
전통적인 시계열 분석 기반 Baseline 모델로 활용한다.  
상대적으로 안정적인 카테고리의 수요 패턴 분석에 적합한지 확인한다.

### XGBoost
Lag Feature, Rolling Mean, 요일 및 월 정보 등 다양한 파생 변수를 활용하여 비선형 수요 패턴을 학습한다.

### LSTM
시계열 데이터의 장기 의존성과 복잡한 패턴을 학습하기 위한 딥러닝 모델로 활용한다.  
다만 프로젝트 범위와 학습 시간을 고려하여 우선적으로 상위 카테고리를 중심으로 적용한다.

---

# 6. 비용 최적화 방식 (경영과학 이론 접목)

예측된 수요를 기반으로 발주량(Order)을 결정하고, 과다 발주와 과소 발주의 비대칭적 손실을 반영한 비용 함수를 통해 최종 운영 비용을 계산한다.

---

## 6-1. 총 비용 함수 (Total Cost Function)

```text
TotalCost = HoldingCost + StockoutCost
```

---

## 6-2. 재고 유지 비용 (Holding Cost)

초과 발주된 재고에 대한 보관 및 기회비용을 계산한다.

```text
HoldingCost = max(Order - Demand, 0) × h
```

- `h`
  - 단위당 재고 유지 비용 파라미터

---

## 6-3. 품절 비용 (Stockout Cost)

수요보다 적게 발주하여 발생한 판매 기회 손실 및 패널티 비용을 계산한다.

```text
StockoutCost = max(Demand - Order, 0) × s
```

- `s`
  - 단위당 품절 손실 비용 파라미터
  - 일반적으로 `s > h` 구조를 가정

---

## 6-4. 모델 평가 방식

본 프로젝트는 단순 예측 정확도뿐 아니라 실제 운영 비용 관점에서도 모델을 평가한다.

### 예측 성능 평가

| 지표 | 의미 |
|---|---|
| SMAPE | 예측 오차 측정 |
| RMSE | 큰 오차에 대한 민감도 평가 |

### 운영 성능 평가

| 지표 | 의미 |
|---|---|
| Holding Cost | 과잉 재고 비용 |
| Stockout Cost | 품절 손실 비용 |
| Total Cost | 총 운영 비용 |

---

# 7. 기대 결과

* 단순 예측 정확도와 실제 운영 효율 간 차이 분석
  * 예측 오차가 낮더라도 특정 모델이 더 높은 운영 비용을 유발할 수 있음을 분석

* 카테고리별 맞춤형 모델 전략 제시
  * 수요 특성에 따라 어떤 모델이 비용 측면에서 유리한지 비교 가능

* 데이터 기반 발주 전략 가능성 검증
  * 예측 기반 의사결정이 기존 단순 발주 방식 대비 비용 절감에 효과적인지 확인

* 데이터사이언스와 경영과학의 융합
  * AI 기반 수요 예측과 비용 최적화 의사결정을 연결한 통합 파이프라인 구축

---

# 8. 향후 계획

| 기간 | 내용 |
|---|---|
| 1주 차 | 시계열 데이터 정제 및 ARIMA/XGBoost 구현 |
| 2주 차 | LSTM 구현 및 모델 성능 비교 |
| 3주 차 | 비용 시뮬레이션, 결과 분석 및 최종 보고서 작성 |

---
