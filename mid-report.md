# 프로젝트 중간 방향 정리

# 프로젝트명
카테고리별 일별 수요 예측 기반 발주량 최적화 시스템

---

# 1. 프로젝트 목적

본 프로젝트의 목적은 이커머스 주문 데이터를 활용하여 카테고리별 일별 수요를 예측하고, 예측 결과를 기반으로 최적 발주량을 결정하여 재고 비용과 품절 비용을 최소화하는 것이다.

기존의 수요 예측 프로젝트는 예측 정확도 자체에만 집중하는 경우가 많았으나, 실제 기업 운영에서는 예측 결과가 재고 관리 및 발주 의사결정에 어떻게 활용되는지가 더욱 중요하다.

따라서 본 프로젝트는 단순 예측 모델 비교를 넘어, 예측 결과가 실제 운영 비용 절감에 얼마나 기여할 수 있는지를 분석하는 것을 목표로 한다.

---

# 2. 프로젝트 핵심 질문

본 프로젝트는 다음 질문을 중심으로 진행된다.

> “수요 예측 모델이 실제 발주 전략과 운영 비용 절감에 얼마나 기여할 수 있는가?”

또한 다음과 같은 세부 질문을 함께 분석한다.

- 카테고리별 수요 패턴은 서로 차이가 존재하는가?
- 예측 정확도가 높을수록 실제 운영 비용도 반드시 감소하는가?
- 어떤 예측 모델이 실제 발주 전략 측면에서 가장 효율적인가?

---

# 3. 분석 대상

본 프로젝트는 Kaggle의 Brazilian E-Commerce Public Dataset by Olist 데이터를 활용한다.

여러 관계형 테이블 중 다음 3개의 핵심 테이블을 중심으로 분석을 진행한다.

| 테이블 | 역할 |
|---|---|
| orders | 주문 날짜 정보 |
| order_items | 주문 상품 및 배송비 정보 |
| products | 상품 카테고리 정보 |

위 데이터를 결합하여 다음 형태의 시계열 데이터를 생성한다.

| date | category | demand |
|---|---|---|
| 2018-01-01 | electronics | 53 |
| 2018-01-01 | toys | 17 |

즉, “카테고리별 일별 주문량”을 예측 대상으로 설정한다.

---

# 4. 진행 내용 (현재까지)

## 4-1. 데이터 구조 분석 (EDA)

현재 데이터 구조 분석(EDA)을 수행하였다.

### 수행 내용

- 주요 테이블 구조 확인
- 결측치 및 데이터 형태 분석
- 주문 날짜 범위 확인
- 카테고리별 주문량 분석
- 일별 주문량 시계열 생성
- 배송비 분포 확인

### 주요 확인 사항

- 카테고리별 주문량 차이가 크게 존재함
- 일부 카테고리는 sparse한 형태를 보임
- 주문량 변동성이 존재하여 수요 예측 필요성이 확인됨
- 배송비 분포 편차가 커 비용 함수에 활용 가능함

---

# 5. 예측 모델 구성

본 프로젝트에서는 다음 모델들을 비교 분석할 예정이다.

| 모델 | 특징 |
|---|---|
| ARIMA | 전통적인 시계열 통계 모델 |
| XGBoost | 비선형 패턴 학습이 가능한 머신러닝 모델 |
| LSTM | 시계열 딥러닝 모델 |

단, 프로젝트 범위와 구현 가능성을 고려하여 우선적으로 ARIMA와 XGBoost를 중심으로 구현을 진행하고, 이후 여유가 있을 경우 LSTM을 추가적으로 적용할 예정이다.

---

# 6. 비용 최적화 방식

예측된 수요를 기반으로 발주량(order quantity)을 결정하고, 다음 비용 함수를 통해 운영 비용을 계산한다.

## 총 비용 함수

\[
TotalCost
=
HoldingCost
+
StockoutCost
\]

---

## 재고 유지 비용

\[
HoldingCost
=
\max(Order - Demand, 0)
\times h
\]

- 초과 발주된 재고에 대한 보관 비용

---

## 품절 비용

\[
StockoutCost
=
\max(Demand - Order, 0)
\times s
\]

- 수요보다 적게 발주하여 발생한 손실 비용

---

이를 통해 단순 예측 정확도뿐 아니라, 실제 운영 비용 측면에서 각 모델의 효율성을 비교할 예정이다.

---

# 7. 기대 결과

본 프로젝트를 통해 다음 내용을 확인하고자 한다.

- 수요 예측 모델이 실제 발주 전략에 미치는 영향
- 예측 정확도와 실제 비용 절감 효과 간의 관계
- 카테고리별 수요 특성 차이
- 데이터 기반 발주 전략의 가능성

또한 데이터사이언스 기반 수요 예측과 경영과학 기반 의사결정 최적화를 연결한 실무형 프로젝트 구조를 구현하고자 한다.

---

# 8. 향후 계획

| 단계 | 내용 |
|---|---|
| 1단계 | 카테고리별 시계열 데이터 정제 |
| 2단계 | Baseline 및 ARIMA 구현 |
| 3단계 | XGBoost 모델 구현 |
| 4단계 | 비용 함수 기반 발주 시뮬레이션 |
| 5단계 | 모델별 비용 비교 및 결과 분석 |
| 6단계 | 최종 보고서 및 시각화 정리 |

---
